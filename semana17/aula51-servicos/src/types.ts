export type User = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type Address = {
   logradouro: string,
   bairro: string,
   localidade: string,
   uf: string
}